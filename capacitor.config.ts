import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sycamoresstake.yw-camp',
  appName: 'Sycamores Stake Youth Conference',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;
